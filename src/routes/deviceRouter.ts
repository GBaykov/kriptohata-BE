import express, {Router} from 'express';
const router: Router = express.Router();

router.get('/')
router.post('/')

router.get('/:id')
router.delete('/:id')
router.put('/:id')

export default router;